# V3.1 Review — Session 3 Handoff

## Context for Incoming Agent (Session 4)

Session 3 applied 14 items (9 new + 5 carried-over). The design doc is nearly locked.

**Two tasks for Session 4, in order:**

### Task 1: Apply "nice-to-have" improvements

Stephen will paste one final round of nice-to-have items. These are lower-priority
improvements — not blockers. For each item:

1. **Validate** against the current design doc (use grep, not line numbers — doc shifts)
2. **Categorize** as: apply (clear improvement), skip (already addressed or unnecessary),
   or decide (needs Stephen's input)
3. **Apply** the clear ones directly to `FromTheBridge_design_v3.1.md`
4. **Update this handoff** with what was applied

### Task 2: Build implementation prompt packets

After the nice-to-haves are done, build the implementation prompts. This is the
primary deliverable — everything before this was preparation.

**Output:** `docs/design/prompts/` — one file per prompt, self-contained with
relevant v3.1 excerpts inlined. The implementing agent should never need to read
the full ~4,800-line doc. Each prompt includes: scope, inputs, constraints, done-when.

**Decomposition pattern per phase:**
1. Infrastructure/DDL prompt — deploy tables/services
2. Per-component build prompts — one adapter/model/endpoint each
3. Verification prompt — run gate criteria

**Phase 1 example breakdown (~14 prompts):**
- P1.0: Dagster service deployment
- P1.1: Tiingo adapter (dependency — first)
- P1.2–P1.10: One per remaining source adapter
- P1.11: GE setup + bronze_core suite
- P1.12: Silver → Gold export asset (gold_observations)
- P1.13: NAS backup jobs + DR baseline
- P1.14: Phase 1 gate verification

**Important:** Start with Phase 1 prompts only. Don't build Phase 2+ prompts yet —
requirements may shift based on Phase 1 learnings.

## What's Done (Sessions 1 + 2)

**35 items resolved across 5 batches + 5 architectural gaps.** Full edit history in
`V3.1_REVIEW_HANDOFF_SESSION2.md`.

### Summary of All Changes

**Batch 1 (6 items):** CR-1 through CR-6 — argon2id fix, asset count, audit_access_log DDL, bronze_archive_log DDL, gold_observations naming, marts.signals_history gate row.

**Batch 2 (10 items):** H-1, H-5, H-10, H-11, H-15, M-2, M-4, M-5, M-10, M-11 — forge_compute spec, Dagster health gates, AssetKey table, metric counts, macro.credit.hy_oas, dead_letter TTL, CFTC PIT, ingested_at gate, GE spec, lifecycle command.

**Batch 3 (4 items):** H-6, H-12, M-1, M-3 — Phase 2 gate criteria, FIRST()→arg_min(), current_values DDL, signal_eligible clarification.

**Batch 4 (4 items):** H-13, H-16, M-7, M-15 — Phase 3 gate §L2.8 reference, pillar count + regime emission gates, neutral threshold gate, signals_history type.

**Batch 5 (9 items):** H-4, H-7, H-8, H-9, H-14, M-6, M-8, M-12, M-14 — ADR-007 table count, Phase 4 shadow artifacts, F1 go-live criteria, 48h preview spec, plan_field_access DDL, signal_snapshot_writer in dependency graph, pillar_attribution scoping, Phase 6 first customer gate.

**Architectural Gaps (5 items):** A-1 DR objectives, A-2 DuckDB concurrency, A-3 credential inventory, A-4 customer identity model, A-5 redistribution decision gate DG-R1.

**design_index.md:** Fully rewritten to agree with v3.1. Points to consolidated doc as single authority. Sources 10→11, redistribution column updated, export cadence corrected, conventions table added.

## Files Modified Across All Sessions

| File | Changes |
|------|---------|
| `docs/design/FromTheBridge_design_v3.1.md` | Sessions 1+2: 35 items. Session 3: 9 items (N-O1, N-C2, N-C1, N-H1–H4, N-O2, N-O3) |
| `docs/design/design_index.md` | Full rewrite to match v3.1 (Session 2) |
| `docs/design/V3.1_REVIEW_HANDOFF_SESSION2.md` | Updated with batches 3-5 + architectural gaps |
| `docs/design/V3.1_REVIEW_HANDOFF_SESSION3.md` | Session 3 items + carried-over open items |
| `CLAUDE.md` | H-11 metric count (Session 1) |
| `db/migrations/clickhouse/0001_silver_schema.sql` | M-2 dead_letter TTL (Session 1) |

## Session 3 Changes Applied

**10 items from final review pass (2026-03-06).** 1 item (N-C3) validated as false positive — no action needed.

### Items Applied to `FromTheBridge_design_v3.1.md`

| ID | Category | Description | Resolution |
|----|----------|-------------|------------|
| N-O1 | Typo | `sosovalve.txt` in secrets directory listing | Fixed to `sosovalue.txt` |
| N-C2 | Credential gap | No MinIO write credential for Marts layer | Added `marts_writer` service account (PutObject on `marts/*`), credential inventory entry, secrets directory files |
| N-C1 | Weight conflict | Thread 2 vs §L2.7 pillar weight tables disagree | Added superseded note to Thread 2 table designating §L2.7 as authoritative for VLA production |
| N-H1 | Taxonomy | `momentum` not in Category A closed set | Changed to `change_pct` with 28d window |
| N-H2 | Undefined classifier | 3-class market regime from undefined ML classifier | Replaced with derivation rule: Macro Regime model `risk_appetite` scalar thresholds (≥0.6 RISK_ON, ≤0.4 RISK_OFF, else TRANSITION) |
| N-H3 | Missing definition | `excess_return_period` in methodology mapping, no metric def | Added full definition: strategy return minus BTC buy-and-hold benchmark, same window as strategy_sharpe, 50 min observations |
| N-O2 | Gate timing | DG-R1 enforcement on Phase 5, too late for Option B | Moved enforcement to Phase 3 gate; added DG-R1 criterion to Phase 3 hard gate table |
| N-H4 | Schema gap | `_redistribution_notice` absent from §L2.8 SignalResponse | Added as optional top-level field typed `{fields_suppressed: int, detail: [{field, status, sources}]} \| None` |
| N-O3 | Bootstrap gap | `audit_access_log` partitioned table has no initial partition | Added bootstrap comment in DDL + deployment step in Phase 5 Track A |
| N-C3 | False positive | DDL table names break FK references | **No action** — DDL already uses `source_catalog`/`metric_catalog` consistently |

### Carried-Over Items Resolved in Session 3

| ID | Description | Resolution |
|----|-------------|------------|
| H1 | Feature compute time unvalidated; Phase 2 gate has no benchmark | Added smoke-test criterion: full feature compute for 10 instruments within 60 min on proxmox (revisable at Phase 3) |
| H2 | `flows.exchange.net_flow_usd` missing from metric catalog | Added to Flows domain listing as derived metric (inflow - outflow), noted as Phase 1 catalog addition |
| H4 | Protocol tier API vs consulting ambiguity | Separated: Protocol = API tier ($2,500–$5,000/mo). Consulting (Stream 3) = separate SKU, custom contracts, not entitlement middleware |
| M1 | `__market__` sentinel not in instruments catalog | Added system-reserved instrument spec: `canonical_symbol = '__market__'`, `collection_tier = 'system'`, Phase 0 seed. Updated instruments DDL CHECK constraint to include 'system' tier |
| M3 | CFTC COT publication lag not in feature spec | Added PIT anchor note in Category A macro features: `ingested_at` (Friday) is PIT anchor, not `observed_at` (Tuesday) |

### Remaining Open Items

| ID | Description | Status | When |
|----|-------------|--------|------|
| C2 (partial) | 3 redistribution open assumptions (items 3, 5, 7 at ~line 2727-2731) | Architect decisions pending | Before Phase 3/5 |
| C3 | EDSx-02/R3 performance history depth audit not yet run | Already documented as Phase 5 Track E blocking step + B3 gate criterion | During Phase 1 |
| M2 | `computed_at` vs `as_of` in signal_outcomes | **No gap** — explicitly documented at line 3154: `as_of` = API wire format of `computed_at` | Closed |
| M4 | ToS outreach not accelerated | External action — not a design doc issue | Ongoing |
| M5 | `hy_oas` active at Phase 0 seed may fail gate | Phase 0 gate passes (catalog row exists). Phase 1 gate explicitly requires FRED adapter collection — already a named criterion | No doc action |

## Review Totals Across All Sessions

| Session | Items Applied | Items Skipped/Closed |
|---------|--------------|---------------------|
| 1 + 2 | 35 (5 batches + 5 architectural gaps) | 0 |
| 3 | 14 (9 new + 5 carried-over) | 1 false positive (N-C3), 3 closed as no-action (M2, M4, M5) |
| **Total** | **49** | **4** |

## Key Facts for Quick Reference

- Design doc: `docs/design/FromTheBridge_design_v3.1.md` (~4,800 lines after Session 3 edits)
- Index: `docs/design/design_index.md`
- Phase 0: Complete. Phase 1: Not started.
- Blocking: Polygon.io integration design session before Phase 1
- Open decision: DG-R1 (redistribution impact) — required before Phase 3 (moved from Phase 5)
- Open decisions: C2 partial — 3 redistribution assumptions at ~line 2727-2731
- `forge.current_values` DDL in design doc but NOT in ClickHouse migration file
- ClickHouse dead_letter TTL in migration file but NOT applied to live DB
- Dagster asset naming convention: noun-form (`collect_{source_id}`)
- `marts.signals_history` = forge_compute Python asset (not dbt)
- DuckDB SQL: `arg_min()` not `FIRST()`
- `__market__` system instrument added — needs Phase 0 seed migration row
- Protocol tier = API only. Consulting = Stream 3, separate SKU.
- `marts_writer` MinIO credential added — needs setup script update
- `flows.exchange.net_flow_usd` = derived metric, Phase 1 catalog addition
- Session 3 changes NOT yet committed to git
